const projects={nextmarga:{label:'01 / FLAGSHIP PRODUCT',title:'NextMarga',description:'A student-first platform for moving from “What opportunities are available to me?” to “What should I do next?” The repository describes opportunity discovery, profiles, roadmaps, applications, dossiers and an AI career-assistance layer.',facts:[['STATUS','Actively developed'],['STACK','React · TypeScript · Vite'],['DATA','Supabase · Auth'],['LIVE','next-marga.vercel.app']],links:[['Visit product','https://next-marga.vercel.app/'],['Source repository','https://github.com/rkaif8314-a11y/NextMarga']]},veriq:{label:'02 / TRUST SYSTEMS',title:'Veriq',description:'A digital trust MVP exploring how websites, messages and claims can communicate signals, uncertainty and recommendations instead of presenting a single opaque answer.',facts:[['STATUS','Exploration'],['FOCUS','Trust · product thinking'],['FORMAT','Web experience']],links:[['Source repository','https://github.com/rkaif8314-a11y/Veriq']]},smartnotepad:{label:'03 / PRODUCT BUILD',title:'SmartNotepad',description:'A cloud note-taking application focused on writing, organization, authentication and persistent user data — a practical exercise in making a small product coherent end to end.',facts:[['STATUS','Built project'],['FOCUS','Auth · cloud data · CRUD'],['FORMAT','Web application']],links:[['Source repository','https://github.com/rkaif8314-a11y/SmartNotepad']]},codelab:{label:'04 / PROBLEM SOLVING',title:'CodeVault',description:'A personal DSA command center built around the Striver A2Z DSA Sheet, with progress, status, analytics and streak-oriented practice.',facts:[['STATUS','Ongoing practice'],['FOCUS','C++ · DSA'],['FORMAT','Learning system']],links:[['Source repository','https://github.com/rkaif8314-a11y/codevault']]}};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
$('#year').textContent=new Date().getFullYear();
const header=$('.header'); addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>30);const max=document.body.scrollHeight-innerHeight;document.documentElement.style.setProperty('--scroll',max>0?scrollY/max:0)},{passive:true});
const menu=$('.menu-button'),nav=$('#nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('#nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const modal=$('#modal'), content=$('#modal-content');let returnFocus;function openProject(key){const p=projects[key];if(!p)return;returnFocus=document.activeElement;content.innerHTML=`<span class="section-index">${p.label}</span><h2 id="modal-title">${p.title}</h2><p>${p.description}</p><div class="modal-facts">${p.facts.map(f=>`<div><small>${f[0]}</small><strong>${f[1]}</strong></div>`).join('')}</div><div class="hero-links">${p.links.map(l=>`<a class="text-link" href="${l[1]}" target="_blank" rel="noopener noreferrer">${l[0]} ↗</a>`).join('')}</div>`;modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';$('.modal-close').focus()}function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow='';returnFocus?.focus()}$$('[data-project]').forEach(el=>{el.addEventListener('click',()=>openProject(el.dataset.project));el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openProject(el.dataset.project)}})});$$('[data-close]').forEach(el=>el.addEventListener('click',closeModal));addEventListener('keydown',e=>{if(!modal.classList.contains('open'))return;if(e.key==='Escape'){closeModal();return}if(e.key!=='Tab')return;const focusable=[...modal.querySelectorAll('a[href],button,input,select,textarea,[tabindex]:not([tabindex="-1"])')].filter(el=>!el.disabled);if(!focusable.length)return;const first=focusable[0],last=focusable[focusable.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}});
const canvas=$('#webgl'),gl=canvas?.getContext('webgl',{alpha:true,antialias:true});const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let mouseX=0,mouseY=0,scroll=0;if(gl&&!reduced){const vs=`attribute vec3 p;attribute float s;uniform float t;uniform vec2 m;uniform float q;varying float a;void main(){vec3 v=p;float d=length(p.xy-m);v.z+=sin(t*0.7+p.x*5.0+p.y*3.0)*0.08+q*0.25;v.xy+=m*0.035*(1.0-d);gl_Position=vec4(v.xy,v.z,1.0);gl_PointSize=s*(1.0+q*.6);a=1.0-d*.35;}`;const fs=`precision mediump float;varying float a;void main(){float d=distance(gl_PointCoord,vec2(.5));if(d>.5)discard;gl_FragColor=vec4(.72,1.0,.2,(1.0-d*2.0)*a);}`;function shader(type,src){const s=gl.createShader(type);if(!s)return null;gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS)){console.warn('Portfolio WebGL shader failed:',gl.getShaderInfoLog(s));gl.deleteShader(s);return null}return s}const vertex=shader(gl.VERTEX_SHADER,vs),fragment=shader(gl.FRAGMENT_SHADER,fs);const prog=vertex&&fragment?gl.createProgram():null;if(prog){gl.attachShader(prog,vertex);gl.attachShader(prog,fragment);gl.linkProgram(prog);if(!gl.getProgramParameter(prog,gl.LINK_STATUS)){console.warn('Portfolio WebGL program failed:',gl.getProgramInfoLog(prog));gl.deleteProgram(prog)}else{const count=170,data=new Float32Array(count*4);for(let i=0;i<count;i++){const a=Math.random()*Math.PI*2,r=Math.sqrt(Math.random())*.92;data[i*4]=Math.cos(a)*r;data[i*4+1]=Math.sin(a)*r;data[i*4+2]=Math.random()*.4-.2;data[i*4+3]=1.5+Math.random()*3}const buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.STATIC_DRAW);const loc=gl.getAttribLocation(prog,'p'),size=gl.getAttribLocation(prog,'s');function resize(){const d=Math.min(devicePixelRatio,1.7),r=canvas.getBoundingClientRect();canvas.width=Math.max(1,r.width*d);canvas.height=Math.max(1,r.height*d);gl.viewport(0,0,canvas.width,canvas.height)}function frame(t){resize();gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);gl.useProgram(prog);gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,3,gl.FLOAT,false,16,0);gl.enableVertexAttribArray(size);gl.vertexAttribPointer(size,1,gl.FLOAT,false,16,12);gl.uniform1f(gl.getUniformLocation(prog,'t'),t*.001);gl.uniform2f(gl.getUniformLocation(prog,'m'),mouseX,mouseY);gl.uniform1f(gl.getUniformLocation(prog,'q'),scroll);gl.drawArrays(gl.POINTS,0,count);requestAnimationFrame(frame)}requestAnimationFrame(frame);canvas.parentElement.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mouseX=(e.clientX-r.left)/Math.max(1,r.width)*2-1;mouseY=-((e.clientY-r.top)/Math.max(1,r.height)*2-1);$('#stage-readout').textContent='TRACKING / POINTER'});addEventListener('scroll',()=>{const max=document.body.scrollHeight-innerHeight;scroll=max>0?Math.min(1,scrollY/max):0;if(scroll>.05)$('#stage-readout').textContent='TRANSFORM / SCROLL'},{passive:true})}}else{canvas.style.display='none'}else{canvas.style.display='none'}
